from setuptools import setup, find_packages

setup(
    name="func",          # nom de ton package
    version="0.1.0",           # version du package
    packages=find_packages(),  # détecte automatiquement le dossier 'fonctions'
)
